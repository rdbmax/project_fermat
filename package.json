{
  "name": "project_fermat",
  "version": "1.0.0",
  "description": "Learn mathematics to children (from 6yo), into the terminal.",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "check": "tsc",
    "start": "npm run check && node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rdb Max <rdbmax@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^22.15.21",
    "inquirer": "^12.6.2"
  },
  "engines": {
    "node": ">=24.0.2"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
